var fs = require('fs');
var makeBuildPatch = require('./patches/makeBuildPatch');

module.exports = function isInstalled(config, name) {
  var buildGradle = fs.readFileSync(config.buildGradlePath);
  return makeBuildPatch(name).installPattern.test(buildGradle);
};