
'use strict';

module.exports = function (url, middleware) {
  return function (req, res, next) {
    if (req.url === url || req.url.startsWith(url + '/')) {
      middleware(req, res, next);
    } else {
      next();
    }
  };
};